# Web Demo

This page keeps the regular docs navigation and theme while rendering the React web demo directly inside VitePress.

<PageQrCode />

<WebDemoMount />

## Notes

- The demo is mounted directly into the docs page, not embedded through an iframe.
- The standalone build is still available for browser automation and direct benchmark deep links.
- Use the standalone page when you want the hash-routed version outside the docs shell.
