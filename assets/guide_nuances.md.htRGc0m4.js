import{_ as a,o as t,c as o,ae as r}from"./chunks/framework.BisYizpz.js";const h=JSON.parse('{"title":"Nuances and edge cases","description":"","frontmatter":{},"headers":[],"relativePath":"guide/nuances.md","filePath":"guide/nuances.md"}'),i={name:"guide/nuances.md"};function n(s,e,d,c,l,u){return t(),o("div",null,[...e[0]||(e[0]=[r('<h1 id="nuances-and-edge-cases" tabindex="-1">Nuances and edge cases <a class="header-anchor" href="#nuances-and-edge-cases" aria-label="Permalink to &quot;Nuances and edge cases&quot;">​</a></h1><p>This page documents behaviors and edge cases that may affect integration or UX.</p><h2 id="input-parsing" tabindex="-1">Input parsing <a class="header-anchor" href="#input-parsing" aria-label="Permalink to &quot;Input parsing&quot;">​</a></h2><h3 id="negative-numbers" tabindex="-1">Negative numbers <a class="header-anchor" href="#negative-numbers" aria-label="Permalink to &quot;Negative numbers&quot;">​</a></h3><p>A leading <code>-</code> is supported (e.g. <code>-1234.56</code>). Minus signs in the middle of the string are stripped; <code>12-3</code> becomes <code>123</code>, <code>--12</code> becomes <code>-12</code>.</p><h3 id="multiple-decimal-points" tabindex="-1">Multiple decimal points <a class="header-anchor" href="#multiple-decimal-points" aria-label="Permalink to &quot;Multiple decimal points&quot;">​</a></h3><p>Extra <code>.</code> characters are collapsed into a single decimal point. For example, <code>12.3.4.567</code> becomes <code>12.34567</code> (first <code>.</code> kept, remainder joined as decimal digits).</p><h3 id="invalid-characters" tabindex="-1">Invalid characters <a class="header-anchor" href="#invalid-characters" aria-label="Permalink to &quot;Invalid characters&quot;">​</a></h3><p>In overlay mode, the typing input is uncontrolled. If the user types letters (e.g. <code>12abc34</code>), they remain visible while focused. On blur, the input remounts and reseeds from the controlled value; letters are stripped and the user sees <code>1234</code> on refocus.</p><h2 id="formatdisplay-and-custom-separators" tabindex="-1">formatDisplay and custom separators <a class="header-anchor" href="#formatdisplay-and-custom-separators" aria-label="Permalink to &quot;formatDisplay and custom separators&quot;">​</a></h2><p><code>formatDisplay</code> can return any string. The library treats only digits, <code>.</code>, and <code>-</code> as significant for:</p><ul><li><strong>Overlay mode</strong>: Mapping click position in the formatted overlay to caret position in the raw typing input.</li><li><strong>Live mode</strong>: Finding the digit to delete when Backspace/Delete skips separators.</li></ul><p>Custom separators (emoji, spaces, other Unicode) work because they are ignored during these operations.</p><h2 id="overlay-mode-reseeding" tabindex="-1">Overlay mode reseeding <a class="header-anchor" href="#overlay-mode-reseeding" aria-label="Permalink to &quot;Overlay mode reseeding&quot;">​</a></h2><p>The overlay mode TypingInput remounts on blur. This ensures that when the user focuses again, the input shows the canonical controlled value rather than stale DOM text (e.g. letters typed before blur). The remount key only updates while blurred, so typing stability is preserved—no caret jump during input.</p><h2 id="platform-differences" tabindex="-1">Platform differences <a class="header-anchor" href="#platform-differences" aria-label="Permalink to &quot;Platform differences&quot;">​</a></h2><ul><li><strong>Web</strong>: <code>inputMode=&quot;numeric&quot;</code> is used; <code>keyboardType</code> is omitted.</li><li><strong>React Native</strong>: <code>keyboardType=&quot;numeric&quot;</code> is used; <code>inputMode</code> is omitted.</li><li><strong>Overlay focus transfer</strong>: On web, the display overlay is focusable and forwards focus to the typing input with caret mapping. On native, the overlay has <code>pointerEvents=&quot;none&quot;</code> and is non-interactive.</li></ul><h2 id="javascript-number-precision" tabindex="-1">JavaScript number precision <a class="header-anchor" href="#javascript-number-precision" aria-label="Permalink to &quot;JavaScript number precision&quot;">​</a></h2><p>JavaScript <code>number</code> precision and <code>Intl.NumberFormat</code> fraction digit limits mean &quot;unlimited decimals&quot; is not truly possible. Large values or many decimal places may lose precision.</p>',19)])])}const m=a(i,[["render",n]]);export{h as __pageData,m as default};
